{
  "name": "cribl-search-app-framework",
  "private": true,
  "description": "Cribl Search App framework monorepo \u2014 shared libraries published to GitHub Packages under @criblio",
  "workspaces": [
    "packages/*"
  ],
  "engines": {
    "node": ">=22.13.0"
  },
  "scripts": {
    "build": "npm run build -w @criblio/agent-protocol && npm run build -w @criblio/app-utils && npm run build -w @criblio/cell-workspace && npm run build -w @criblio/cell-harness"
  }
}
