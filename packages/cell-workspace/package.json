{
  "name": "@criblio/cell-workspace",
  "version": "0.1.0",
  "type": "module",
  "description": "Source-code workspace for cells (server-side agents on celld): lazy tarball checkout into Durable Object SQLite plus the read-only code tools over it. Worker-native — no filesystem, no subprocess.",
  "main": "src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "dependencies": {
    "@criblio/agent-protocol": "file:../agent-protocol"
  },
  "peerDependencies": {
    "@cribl/app-utils": "*"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250705.0",
    "@cribl/app-utils": "file:../app-utils",
    "typescript": "^6.0.3",
    "vitest": "^3.2.7"
  },
  "scripts": {
    "test": "vitest run",
    "typecheck": "tsc --noEmit"
  }
}
