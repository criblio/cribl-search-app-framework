# Module constraints only — the consuming root supplies the provider
# configuration and the state backend.
terraform {
  required_version = ">= 1.6"
  required_providers {
    aws = {
      source  = "hashicorp/aws"
      version = "~> 6.0"
    }
  }
}
